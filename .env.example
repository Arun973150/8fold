# Optional: enables the grounded LLM résumé extractor (LangChain + Groq).
# Copy this file to .env and fill in your key. Without it, the pipeline runs on the
# deterministic heuristic parser only.
GROQ_API_KEY=
# Optional override; defaults to llama-3.3-70b-versatile
# GROQ_MODEL=llama-3.3-70b-versatile
