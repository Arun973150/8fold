__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/
.DS_Store
*.db

# secrets — never commit the real key (.env.example is committed as a template)
.env

# user-uploaded résumés from the UI (personal data)
uploads/

# personal / real resumes dropped in the repo root — never commit to a public repo
/*.pdf
/*.docx
!samples/resumes/*
# local AI/tool state
.claude/
.benchmarks/

# personalized generated submission; keep docs/DESIGN.pdf
docs/*_Eightfold.pdf

